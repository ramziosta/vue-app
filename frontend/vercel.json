{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/node",
      "config": {
        "engines": {
          "node": "20.x"
        }
      }
    }
  ],
  "outputDirectory": "frontend/dist"
}